diff --git a/target/ia64/rse.c b/target/ia64/rse.c
index 2e40adb..b451cde 100644
--- a/target/ia64/rse.c
+++ b/target/ia64/rse.c
@@ -9,16 +9,30 @@
 
 #define IA64_RSE_CFM_MASK ((UINT64_C(1) << 46) - 1)
 
-int ia64_rse_find_return_frame(const CPUIA64State *env,
+static bool ia64_rse_view_valid(const struct IA64RSEReturnFrameView *view)
+{
+    if (!view || !view->base || view->count == 0 ||
+        view->count > INT_MAX || view->stride < sizeof(uint64_t) ||
+        view->count > SIZE_MAX / view->stride) {
+        return false;
+    }
+
+    return view->cfm_offset <= view->stride - sizeof(uint64_t) &&
+           view->ret_addr_offset <= view->stride - sizeof(uint64_t);
+}
+
+int ia64_rse_find_return_frame(const struct IA64RSEReturnFrameView *view,
                                uint64_t ret_addr, uint64_t pfs_cfm)
 {
+    const uint8_t *base;
     uint64_t target = ret_addr & ~UINT64_C(0xf);
     uint64_t caller_cfm = pfs_cfm & IA64_RSE_CFM_MASK;
     int index;
 
-    if (!env->rse_frames || env->rse_depth == 0 || target == 0) {
+    if (!ia64_rse_view_valid(view) || target == 0) {
         return -1;
     }
+    base = view->base;
 
     /*
      * The return address is the strongest identity component, but it is not
@@ -29,12 +43,17 @@ int ia64_rse_find_return_frame(const CPUIA64State *env,
      * Search newest-to-oldest so indistinguishable recursive activations keep
      * the ordinary single-pop behavior instead of skipping an inner frame.
      */
-    for (index = (int)env->rse_depth - 1; index >= 0; index--) {
-        const struct IA64RSEFrame *frame = &env->rse_frames[index];
+    for (index = (int)view->count - 1; index >= 0; index--) {
+        const uint8_t *frame = base + (size_t)index * view->stride;
+        uint64_t frame_cfm;
+        uint64_t frame_ret_addr;
 
-        if (frame->ret_addr != 0 &&
-            (frame->ret_addr & ~UINT64_C(0xf)) == target &&
-            (frame->cfm & IA64_RSE_CFM_MASK) == caller_cfm) {
+        memcpy(&frame_cfm, frame + view->cfm_offset, sizeof(frame_cfm));
+        memcpy(&frame_ret_addr, frame + view->ret_addr_offset,
+               sizeof(frame_ret_addr));
+        if (frame_ret_addr != 0 &&
+            (frame_ret_addr & ~UINT64_C(0xf)) == target &&
+            (frame_cfm & IA64_RSE_CFM_MASK) == caller_cfm) {
             return index;
         }
     }
diff --git a/target/ia64/rse.h b/target/ia64/rse.h
index ef930f2..ea6733b 100644
--- a/target/ia64/rse.h
+++ b/target/ia64/rse.h
@@ -6,7 +6,18 @@
 #ifndef TARGET_IA64_RSE_H
 #define TARGET_IA64_RSE_H
 
-#include "cpu.h"
+/*
+ * Describe the return-identity fields inside an array of shadow frames.
+ * Keeping this view independent of CPUIA64State lets common-code unit tests
+ * exercise the selector without including target-only cpu.h.
+ */
+struct IA64RSEReturnFrameView {
+    const void *base;
+    size_t count;
+    size_t stride;
+    size_t cfm_offset;
+    size_t ret_addr_offset;
+};
 
 /*
  * Locate the shadow call frame selected by a br.ret.
@@ -16,7 +27,7 @@
  * Either component alone is ambiguous.  Return the newest exact match, or -1
  * when the caller must retain the ordinary single-pop behavior.
  */
-int ia64_rse_find_return_frame(const CPUIA64State *env,
+int ia64_rse_find_return_frame(const struct IA64RSEReturnFrameView *view,
                                uint64_t ret_addr, uint64_t pfs_cfm);
 
 #endif /* TARGET_IA64_RSE_H */
diff --git a/tests/unit/test-ia64-rse.c b/tests/unit/test-ia64-rse.c
index dfe4115..d59dd3f 100644
--- a/tests/unit/test-ia64-rse.c
+++ b/tests/unit/test-ia64-rse.c
@@ -5,10 +5,20 @@
  */
 
 #include "qemu/osdep.h"
-#include "qemu/typedefs.h"
 #include "target/ia64/rse.h"
 
-static void init_frames(CPUIA64State *env,
+struct IA64RSEFrame {
+    uint64_t padding;
+    uint64_t cfm;
+    uint64_t ret_addr;
+};
+
+typedef struct TestIA64RSEState {
+    struct IA64RSEFrame *rse_frames;
+    size_t rse_depth;
+} TestIA64RSEState;
+
+static void init_frames(TestIA64RSEState *env,
                         struct IA64RSEFrame *frames,
                         size_t count)
 {
@@ -18,16 +28,30 @@ static void init_frames(CPUIA64State *env,
     env->rse_depth = count;
 }
 
+static int find_return_frame(const TestIA64RSEState *env,
+                             uint64_t ret_addr, uint64_t pfs_cfm)
+{
+    const struct IA64RSEReturnFrameView view = {
+        .base = env->rse_frames,
+        .count = env->rse_depth,
+        .stride = sizeof(*env->rse_frames),
+        .cfm_offset = offsetof(struct IA64RSEFrame, cfm),
+        .ret_addr_offset = offsetof(struct IA64RSEFrame, ret_addr),
+    };
+
+    return ia64_rse_find_return_frame(&view, ret_addr, pfs_cfm);
+}
+
 static void test_empty(void)
 {
-    CPUIA64State env = { 0 };
+    TestIA64RSEState env = { 0 };
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x1000, 0x301), ==, -1);
+    g_assert_cmpint(find_return_frame(&env, 0x1000, 0x301), ==, -1);
 }
 
 static void test_top_exact_match(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[2];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -36,12 +60,12 @@ static void test_top_exact_match(void)
     frames[1].ret_addr = 0x2000;
     frames[1].cfm = 0x302;
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x2002, 0x302), ==, 1);
+    g_assert_cmpint(find_return_frame(&env, 0x2002, 0x302), ==, 1);
 }
 
 static void test_nonlocal_return_address_wins(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[3];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -56,12 +80,12 @@ static void test_nonlocal_return_address_wins(void)
      * The newest frame deliberately has the same CFM.  Only the older frame
      * matches both architectural identity components.
      */
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x3001, 0x30a), ==, 1);
+    g_assert_cmpint(find_return_frame(&env, 0x3001, 0x30a), ==, 1);
 }
 
 static void test_conflicting_components_do_not_match(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[3];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -76,12 +100,12 @@ static void test_conflicting_components_do_not_match(void)
      * The return address and PFS each match a frame, but not the same frame.
      * Discarding either frame would turn inconsistent state into corruption.
      */
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x2001, 0x38a), ==, -1);
+    g_assert_cmpint(find_return_frame(&env, 0x2001, 0x38a), ==, -1);
 }
 
 static void test_return_address_only_does_not_match(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[2];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -90,12 +114,12 @@ static void test_return_address_only_does_not_match(void)
     frames[1].ret_addr = 0x2000;
     frames[1].cfm = 0x302;
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x2002, 0x777), ==, -1);
+    g_assert_cmpint(find_return_frame(&env, 0x2002, 0x777), ==, -1);
 }
 
 static void test_pfs_only_does_not_match(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[2];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -104,12 +128,12 @@ static void test_pfs_only_does_not_match(void)
     frames[1].ret_addr = 0x2000;
     frames[1].cfm = 0x302;
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x9000, 0x302), ==, -1);
+    g_assert_cmpint(find_return_frame(&env, 0x9000, 0x302), ==, -1);
 }
 
 static void test_duplicate_exact_match_prefers_newest(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[3];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -120,24 +144,24 @@ static void test_duplicate_exact_match_prefers_newest(void)
     frames[2].ret_addr = 0x2000;
     frames[2].cfm = 0x30a;
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x2001, 0x30a), ==, 2);
+    g_assert_cmpint(find_return_frame(&env, 0x2001, 0x30a), ==, 2);
 }
 
 static void test_zero_target_does_not_match(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[1];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
     frames[0].ret_addr = 0;
     frames[0].cfm = 0x201;
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0, 0x201), ==, -1);
+    g_assert_cmpint(find_return_frame(&env, 0, 0x201), ==, -1);
 }
 
 static void test_no_match(void)
 {
-    CPUIA64State env;
+    TestIA64RSEState env;
     struct IA64RSEFrame frames[2];
 
     init_frames(&env, frames, G_N_ELEMENTS(frames));
@@ -146,7 +170,7 @@ static void test_no_match(void)
     frames[1].ret_addr = 0x2000;
     frames[1].cfm = 0x302;
 
-    g_assert_cmpint(ia64_rse_find_return_frame(&env, 0x9000, 0x777), ==, -1);
+    g_assert_cmpint(find_return_frame(&env, 0x9000, 0x777), ==, -1);
 }
 
 int main(int argc, char **argv)
diff --git a/target/ia64/helper.c b/target/ia64/helper.c
--- a/target/ia64/helper.c
+++ b/target/ia64/helper.c
@@ -17634,7 +17634,14 @@ void HELPER(ret_restore)(CPUIA64State *env)
      * window and can corrupt gp before the branch is taken.
      */
     if (env->rse_depth > 0) {
-        int selected = ia64_rse_find_return_frame(env, b0, pfs_cfm);
+        const struct IA64RSEReturnFrameView view = {
+            .base = env->rse_frames,
+            .count = env->rse_depth,
+            .stride = sizeof(*env->rse_frames),
+            .cfm_offset = offsetof(struct IA64RSEFrame, cfm),
+            .ret_addr_offset = offsetof(struct IA64RSEFrame, ret_addr),
+        };
+        int selected = ia64_rse_find_return_frame(&view, b0, pfs_cfm);
         int dropped = 0;
 
         if (selected >= 0) {
